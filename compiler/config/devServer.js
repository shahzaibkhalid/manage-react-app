module.exports = {
  /**
   * @open
   * To open the development environment URL in the browser
   * after development server is up.
   */
  open: true,
  /**
   * @hot
   * To enable Hot Module Replacement, this flag needs to be
   * enabled apart from setting React's Fast Refresh plugins etc.
   */
  hot: true,
  noInfo: true,
}
