/**
 * '@testing-library/jest-dom' includes set of matchers functions
 * exclusively designed to run assertions on DOM nodes, like
 * `toHaveClass` and `toBeInTheDocument` etc.
 */
require('@testing-library/jest-dom');
